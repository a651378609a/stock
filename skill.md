---
name: self-evolving-long-only
description: 自进化场内 ETF 多头交易技能。冻结执行壳负责防穿越、仅 ETF、交易明细与依据、硬顶风控、日更/进化/独立监督；可进化参数仅在 strategy_genome.md。
---

# 自进化多头技能（冻结执行壳）

本文件是**冻结壳**：防穿越、仅 ETF、交易日志、硬顶、流程与产出契约。  
可进化内容只允许出现在 `strategy_genome.md`（策略基因）。进化代理**不得**改本文件。  
**监督代理**只审计、可一票否决晋级，也不得改本壳。

配套：
- `strategy_genome.md` — 唯一可进化物
- `state/` — 持仓、权益、信息包、决策
- `evolution/` — 窗口、候选、**交易明细日志**、晋级日志
- `evolution/audit/` — 监督代理审计结论（防穿越与路径合规）

---

## 0. 宪法（不可进化）

### 0.1 目标与晋级

- **不划分牛/熊标签**。
- 进化信号分两路；**有足够实盘样本时，日更反思权重大于历史回溯**：
  - `权重_实盘` 默认 **0.70**；`权重_历史` 默认 **0.30**（有实盘样本时使用，合计为 1）。
  - **冷启动 / 实盘不足**：当现任基因上的连续纸交易日数 < `最少实盘交易日`（默认 **20**）时，**不阻塞晋级**。此时「纯历史晋级」：综合分 = `预测年分`；只执行预测年/压力月门槛；日更仍写反思供日后加权。
  - **实盘就绪**：交易日数 ≥ 20 后，综合分 = `0.70×实盘分 + 0.30×预测年分`，并启用双轨回撤门槛。**禁止**为抬预测年分而人为下调 `权重_实盘`。
- 代际晋级（评估器冻结）：
  1. 综合分高于现任；
  2. **预测年回撤硬门槛**：2026 年评估段最大回撤 ≤ 现任 × (1+ε)，ε = **0.10**；
  3. **预测年全年收益门槛**（见 §0.1b）：全年（或迄今）扣成本收益不得显著差于现任；
  4. **压力月门槛**（见 §0.1b）；
  5. **实盘回撤硬门槛**（仅实盘就绪时）。
- `预测年分`：预测年 2026 评估段扣成本年化（方法冻结）。
- `实盘分`：纸交易任期内扣成本年化 + 合规加减分；未就绪不参与综合分、不阻塞纯历史晋级。
- 成本：佣金双边万三；印花税卖出千一；滑点 0.1%。成交：信号日 T → **T+1 开盘价**。

### 0.1b 预测年 2026 与压力月（冻结）

| 层级 | 区间 | 作用 |
|------|------|------|
| **预测年（主评测）** | 2026-01-01～2026-12-31（或数据已封闭的迄今末日） | 必须单独报告：**全年/迄今扣成本收益、年化、最大回撤、成交次数** |
| 压力月 | 2026-04、2026-07 各自然月 | 在全年之外再单独出表，不得只看全年平均掩盖单月崩盘 |

规则：

1. **训练 / 进化可用数据硬截断**：一切训练、热身标定、进化段回放，只允许使用 **≤ 2025-12-31** 的数据。开始训练时**不得看见**预测年（2026）及之后的价量。
2. **预测年只用于评测与晋级门槛**，不得把预测年内的事后涨跌写进策略基因条款。
3. 压力月门槛：候选各月最大回撤 ≤ 现任同月 × (1+ε)；现任无样本时单月回撤 ≤ **25%**。
4. 全年门槛：候选预测年扣成本收益 ≥ 现任同段收益 − 容忍带（默认与 ε 同级的相对容忍，评估器冻结）；并报告全年收益数字。
5. 日更落在压力月须标注「压力月样本」。

### 0.2 时间协议（防穿越）

- **训练截止日**：`2025-12-31`。进化段必须整段落在截止日及之前。
- **预测年**：`2026`，仅作样本外评测。
- 训练期内可做滚动前进（约 24 个月进化可用 → 期内验证），但**任何窗口终点不得越过训练截止日**。
- 改基因理由不得引用预测年/压力月的事后涨跌路径。
- 盘前决策只用 **当日 09:00 前** 已存在盘面信息（见 §2）；**不再使用消息面字段**（避免空消息导致推理不一致）。

### 0.3 禁点名与禁正股（壳 + 策略基因同样生效）

**禁止交易与持有 A 股正股**（含可转债等非 ETF 品种）。宇宙仅限**场内 ETF**。

规则层（`skill.md` / `strategy_genome.md` / 进化差异理由）禁止：

- 具体正股名 / 正股代码；
- 把细分行业/概念写成**固定偏好或永久优先级**；
- 把某一只 ETF 写成「永远必买/永不满仓」。

允许：

- **抽象 ETF 角色**：进攻成长 / 次线卫星 / 防御红利或低波 / 宽基工具 / 货币或短债 / 现金；
- **运行时**在产出与交易日志中写明具体 ETF 名称与代码（执行结果，不是规则）；
- 评估器内部的「角色→流动性 ETF 报价代理」映射表（`evolution/data/etf_role_map.json`），不得抄进策略基因当偏好清单。

### 0.4 交易宇宙与硬顶（冻结）

- 宇宙：**仅 A 股场内 ETF，多头**；可空仓；无融资融券、无港美、**无正股**。
- 硬顶：单只 ETF ≤ **20%**；总仓 ≤ **95%**；单日单向调仓 ≤ **30%**；现金可达 **100%**。
- 评估初始资金：**100 万人民币**；盲测窗现金重启；纸交易账户连续结转。

### 0.5 文件职责

| 文件 | 可否改 | 内容 |
|------|--------|------|
| `skill.md` | 否 | 本壳 |
| `strategy_genome.md` | 仅进化流程 | 战术参数与抽象触发 |
| `state/*` | 日更/进化 | 持仓、权益、信息包、决策 |
| `evolution/log/*` | 进化 | 代际摘要 |
| `evolution/trades/*` | 进化/日更 | **逐笔交易明细与依据（必填）** |
| `evolution/audit/*` | 仅监督代理 | 防穿越与路径审计 |

---

## 1. 决策哲学（冻结骨架，非点名）

执行时带着这些可检验原则（细节幅度读策略基因）：

1. **保本金优先**：不确定则偏不操作；宁可不做，不两头挨打。
2. **高盈亏比**：试错仓小；趋势确认后再加；破逻辑/破风控则走。
3. **资金先于叙事**：主题由资金连续流入 + 可验证催化长出，不由预设清单决定。
4. **角色清晰**：每笔 ETF 持仓必须标明抽象角色；角色决定持有节奏。
5. **马太与物极**：强者可更强，但已拥挤且失效时切换；无人问津且逻辑在时才考虑逆向。
6. **默认不操作**：无明确动机时输出「今日不操作」并写原因。
7. **先依据、后下单**：任何买入/卖出必须能追溯到**信号日及之前**的盘面证据（见 §2.6）。**不要求、不填写消息面**（已废除，避免空字段造成推理不一致）。

---

## 2. 日更流程（纸交易 + 信息包）

触发：交易日日程（由自动化任务调度）。默认在约定时刻运行。

### 2.1 按日信息包（防软穿越）

每个交易日维护一份 `state/info_packs/YYYY-MM-DD.json`，只含 **T 日 09:00 前可见** 的**盘面结构化数据**：

- 交易日历；宽基/风格指数与候选场内 ETF 的前收/开高低收（仅已收盘日）；
- ETF/板块资金流向（已对齐日期）；
- 持仓 ETF 近况（价量、溢价折价、流动性）。

**不再采集或依赖消息面/新闻正文。**  
评估只认「盘面包完整」的交易日。

### 2.2 盘前扫描（仅 ETF + 仅盘面）

扫描持仓 ETF 状态、角色内相对强弱、资金连续性、是否需在 ETF 角色间平换。  
禁止改买正股。无增量则跳过。

### 2.3 决策与成交记账

1. 加载 `state/portfolio.json` 与 `strategy_genome.md`（持仓只能是 ETF）。
2. 用 §1 + 策略基因生成**仅 ETF**调仓；每笔满足 §2.6 与 §5。
3. 强制硬顶；越界则削减或取消。
4. 信号在 T，默认 T+1 开盘成交 + 成本。
5. 更新持仓、权益、`state/decisions/YYYY-MM-DD.md`，并追加 `evolution/trades/`（§2.6）。
6. 无动机 → 「今日不操作」+ 原因。
7. 必做 §2.5 反思。

### 2.6 交易明细与依据（冻结，仅盘面）

**没有盘面依据的交易视为无效**；日志若只有参数表没有逐笔明细，监督必须判不合格。

每笔写入 `evolution/trades/`，至少包含：

| 字段 | 要求 |
|------|------|
| 信号日 / 成交日 | 信号 T，成交默认 T+1 |
| ETF 名称与代码 | 场内 ETF；禁止正股 |
| 操作 / 仓位 / 价格 | 开加减小清或平换 + 数字 |
| 角色 | 进攻成长/次线/防御/工具/货币等 |
| 盘面依据 | **仅用信号日及之前**价量、均线/相对强弱、资金流等，写清观察窗口 |
| 动机形态 | 对应 §2.4 |
| 基因条款 | 可选 |

**禁止出现「消息面依据」字段。**  
禁止用成交日之后才知道的涨跌倒推；禁止只写「按模型调仓」而无盘面要点。

### 2.5 日更自进化反思（权重大于历史回溯）

每个交易日在决策与记账后，写入 `evolution/live_reflections/YYYY-MM-DD.md`（及可选结构化摘要）。只许用**当日及此前**已实现信息，禁止用尚未发生的涨跌倒推。

反思必须结构化（禁止点名写回规则）：

1. **决策回顾**：今日操作/不操作的动机形态是否与策略基因一致；是否撞硬顶。
2. **已实现结果**：仅评估「昨日及更早信号」在今日可得价格下的实现盈亏（T+1 开盘规则），更新 `state/live_scorecard.json`（任期内扣成本年化、最大回撤、交易日数、胜率、违规次数）。
3. **误差归因（抽象）**：如风控过慢、换手过高、空仓过久、追涨倾向、该动未动——**不得**写成「应增加某细分/某股票」。
4. **突变提案（非生效）**：0–3 条对策略基因的**抽象**改参建议（字段级，如「G4.自高点强制止损」），写入 `evolution/live_proposals/`；标记来源为「实盘」、权重提示为「高」。提案**不直接改** `strategy_genome.md`，只供进化自动化按 §3.4 高权重采纳或拒绝。
5. **反例约束**：若今日反思与历史盲测归因冲突，注明冲突；进化侧默认**偏信实盘**（见 §3.4）。

### 2.4 卖/买/清/不操作动机（形态层，幅度在策略基因）

以下为**形态名**，禁止在此填写具体行业/股票：

**倾向卖/减**：目标到达；催化弱于预期；单票占比触及策略基因减压带；日内加仓层失效做 T；基本面瑕疵（业绩雷）；估值压制；主题资金连续流出；同主题弱换强；风控线触发。

**倾向买/加**：新方向建底仓；主线扩散至相对低位第二梯队；降波动需求开防御角色；补涨预期差；同主题换更高壁垒或换业绩可验证或换容量；除权后可参与的容量替代；分歧抗跌试错；关键均线/位置低吸（参数见策略基因）；早盘弱势承接（高开冲高不追）。

**倾向清**：止盈完成且技术失效；持续弱于同主题；逻辑破灭；合规/风控禁止；资金连续流出确认。

**倾向不动**：持仓逻辑与技术未坏；主题分歧未明；混沌期降频；无腾位需求且总仓已近优。

清 = 离场；减 = 落袋。逻辑退出不要写成象征性小减。

---

## 3. 进化流程（持续跑）

由独立自动化任务持续调度：

1. 消化日更反思 / 实盘提案（若有）；
2. 在**训练截止日及之前**的数据上做进化段滚动；
3. 在**预测年 2026**上做样本外评分（全年 + 压力月）。

### 3.1 窗口状态

维护 `evolution/window.json`：

- `训练截止日`：2025-12-31（不可改）
- `进化段`起止：必须整段 ≤ 训练截止日（约 24 个月）
- `预测年`：2026；`预测段`起止：2026-01-01～min(2026-12-31, 已封闭末日)
- 现任基因版本、实盘交易日数、晋级模式、待监督确认

**铁律**：加载训练/进化面板时，价量序列在训练截止日之后必须对训练路径不可见（截断或隔离）。预测年数据仅进入评测函数。

### 3.2 一代步骤

1. **检查器**：壳未被改；无正股点名；无引用预测年事后涨跌的改参理由。
2. **突变**：实盘就绪且有文档时 ≥3/5 响应实盘；否则允许 5/5 来自训练期抽象归因。不得因缺实盘分跳过晋级。
3. **评估**（现金每段重启 100 万；T→T+1 开盘 + 成本）：
   - `预测年分`：预测段全年/迄今回放；
   - **全年收益表** + **压力月 2026-04/07 分项**；
   - 可选：训练期内验证分（不得替代预测年分）；
   - `实盘分`：就绪则算，否则「未就绪」。
4. **晋级**：过预测年回撤、全年收益、压力月门槛后取综合分最高者；标记待监督确认。
5. **右移**：仅在训练期内右移进化窗；预测年锚点不变。
6. **落盘**：日志含模式、预测年全年指标、压力月表、交易明细路径（仅盘面依据）、待监督确认。

### 3.3 归因隔离

可读抽象错误归因与日更提案。  
不可把预测年个股/ETF 事后涨跌倒推写进基因；不可写消息面字段。

### 3.4 实盘优先冲突决议

仅实盘就绪且存在提案时：默认偏信实盘生成 ≥3/5 候选；晋级仍看预测年全年 + 压力月 + 回撤门槛。  
冷启动以预测年分 + 全年/压力月门槛为主。

---

## 4. 选股与组合（执行时动态，规则层不点名）

开仓前在候选上过四维（具体阈值读策略基因）：

1. **基本面**：地位清晰、业绩或订单可验证、非纯一日游炒作  
2. **技术面**：回调企稳 / 区间下沿 / 突破回踩等形态（参数在策略基因）  
3. **资金面**：主题资金具连续性，而非单日脉冲  
4. **角色互补**：进攻/防御/周期/工具波动互补，避免单一角色堆满硬顶  

选股时综合：容量（流动性与可加仓性）、弹性、熟悉度（自身持仓史）、回避已拥挤且失效的龙头。  
列出 2–3 个同主题候选并写排除理由，再下单——主题名来自当日数据，不来自策略基因清单。

---

## 5. 产出契约

### 5.1 调仓指令（每笔五要素）

```
【组合】调仓  HH:MM
【操作类型】【股票名】，仓位变化，指引价（买入时给）
原因：一句话（对应动机形态名 + 当日事实）
角色：进攻|防御|周期|工具
```

必须包含：股票名、操作类型、仓位变化数字、买入指引价、盘中时序。  
禁止只给方向不给数字。

### 5.2 日更结束时还必须写

- 当日信息包路径与是否完整  
- 持仓表快照（代码、占比、成本、角色、目标/止损）  
- 权益与回撤  
- 若无操作：明确「今日不操作」+ 原因  
- **§2.5 反思文件** + 更新后的实盘记分卡 + 可选实盘提案目录  

### 5.3 进化结束时还必须写

- 晋级模式：纯历史 / 实盘加权；训练截止日与预测段  
- 各候选：预测年分、实盘分或「未就绪」、综合分、预测年回撤  
- **预测年全年/迄今**：扣成本收益、年化、最大回撤、成交次数  
- **压力月分项**：2026-04、2026-07  
- 交易明细路径（仅盘面依据）；待监督确认；晋级或保持原因  

---

## 6. 自动化分工

| 自动化 | 节奏 | 职责 |
|--------|------|------|
| 日更 | 工作日约定时刻 | §2：仅 ETF + 盘面信息包 + 交易明细 + 反思 |
| 进化 | 持续调度 | §3：训练≤2025-12-31；评测预测年2026全年+压力月 |
| **监督** | 错峰每小时 | §8：防穿越（含训练偷看预测年）与路径合规 |

日更不改基因；进化不改壳；监督不改壳与基因内容，只写 `evolution/audit/` 并在否决时回滚未监督确认的晋级。

---

## 7. 免责

产出基于公开信息与模型推断，仅供研究与纸交易，不构成投资建议。实盘风险自担。

---

## 8. 独立监督代理（冻结，最重要）

必须由**与进化/日更分离**的自动化任务执行。监督代理不得参与突变提案，不得为抬分而改规则。

### 8.1 防穿越审计（最高优先级）

对最近一代日志、交易明细、信息包抽样核查：

1. 训练/进化是否使用了 **>2025-12-31** 的数据做训练或热身偷看；
2. 每笔交易盘面依据是否仅用信号日及之前；
3. 是否仍出现已废除的「消息面依据」空字段或假填；
4. 改基因理由是否引用预测年/压力月事后涨跌倒推；
5. 是否出现正股成交。

任一条成立 → **否决**；必要时回滚基因并写证据。

### 8.2 优化路径合规

- 晋级模式正确；未因无实盘分错误禁晋级；
- 仅 ETF；硬顶未破；
- 有逐笔盘面依据明细；
- **预测年全年/迄今指标已出表**，且压力月 2026-04/07 已单独出表；
- 综合分/门槛与 §0.1 / §0.1b 一致。

### 8.3 产出

写入 `evolution/audit/YYYYMMDDTHHMMSSZ_audit.md`（及 json）：

- 结论：通过 / 否决
- 防穿越证据（文件路径 + 摘录）
- 路径合规逐条勾选
- 对进化自动化的强制动作：允许晋级生效 / 否决并回滚 / 要求补交易明细后重跑

**未经最近一次「通过」审计，晋级不得视为最终生效。**
