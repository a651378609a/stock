---
name: self-evolving-long-only
description: 自进化 A 股多头交易技能。冻结执行壳负责防穿越、禁点名、硬顶风控与日更/进化流程；可进化战术参数仅存在于 strategy_genome.md。用于盘前纸交易决策、按日信息包落盘，以及滚动前进自进化晋级。
---

# 自进化多头技能（冻结执行壳）

本文件是**冻结壳**：防穿越、禁点名、硬顶、流程与产出契约。  
可进化内容只允许出现在 `strategy_genome.md`（策略基因）。进化代理**不得**改本文件。

配套：
- `strategy_genome.md` — 唯一可进化物（角色预算、加减仓步长、抽象触发形态）
- `state/` — 运行时状态（持仓、权益、信息包、代际记录）；由自动化任务维护
- `evolution/` — 滚动前进窗口状态与候选基因评估日志

---

## 0. 宪法（不可进化）

### 0.1 目标与晋级

- **不划分牛/熊标签**。
- 进化信号分两路，**日更纸交易反思权重大于历史回溯**：
  - `权重_实盘`（连续纸交易账户上的前瞻表现 + 日更反思）默认 **0.70**
  - `权重_历史`（滚动前进历史盲测）默认 **0.30**
  - 权重冻结在评估器；合计为 1；**禁止**为抬历史分而下调 `权重_实盘`。
- 代际晋级（评估器冻结）：
  1. **综合分** = `权重_实盘 × 实盘分 + 权重_历史 × 历史分` 高于现任；
  2. **双轨回撤硬门槛**均须通过：历史盲测最大回撤 ≤ 现任历史 × (1+ε)，且连续纸交易账户最大回撤 ≤ 现任纸交易 × (1+ε)，ε = **0.10**；
  3. 若连续纸交易样本不足 `最少实盘交易日`（默认 **20** 个交易日），则该代**只允许**积累日更反思与历史窗推进，**不得晋级**（避免无实盘权重时偷用纯历史刷分）。
- `历史分`：盲测段扣成本后的年化收益（标准化到与实盘分同尺度，方法冻结在评估器）。
- `实盘分`：连续纸交易账户在「当前基因任期内」的扣成本年化收益，并结合日更结构化反思的合规扣分/加分（见 §2.5、§3.4）；**不得**用当日未实现的未来涨跌重写历史决策。
- 成本假设（评估器冻结，策略基因不可改）：佣金双边万三；印花税卖出千一；滑点 0.1%。
- 成交假设（评估主路径）：信号日 T → **T+1 开盘价**成交，再叠加上述成本。

### 0.2 时间协议（防穿越）

- **严格滚动前进**：进化可用段约 24 个月 → 盲测 6 个月 → 窗口右移 6 个月。
- 改策略基因的理由**不得**引用盲测段内的涨跌结果或未来信息。
- 进化可用**全部可得的历史 A 股数据**，但必须按滚动前进切开；禁止用全样本一次性拟合后再在同段上宣称盲测。
- 盘前决策只能使用 **当日 09:00 前已存在** 的信息（见 §2）。

### 0.3 禁点名（壳 + 策略基因同样生效）

禁止在 `skill.md`、`strategy_genome.md`、进化差异、评估归因的**规则层**出现：

- 具体股票名 / 股票代码；
- 细分行业名、概念板块名、主题炒作专名作为**固定偏好或优先级**。

允许：

- **抽象角色**：主线进攻 / 次线配置 / 防御 / 周期 / 现金 / 宽基或货币类工具（角色，不点名标的）；
- **运行时动态主题名**：由当日搜索/数据长出，只出现在当日产出与信息包，**不得写回规则文件**；
- 产出指令里的具体股票名（那是执行结果，不是规则）。

检查器拒收任何试图把「某细分永远优先于另一细分」写进策略基因的变更。

### 0.4 交易宇宙与硬顶（冻结）

- 宇宙：**A 股正股 + 场内 ETF，仅多头**；可空仓；无融资融券、无港美仓。
- 硬顶（策略基因只能在顶内活动）：
  - 单票 ≤ **20%**
  - 总仓 ≤ **95%**
  - 单日单向调仓合计 ≤ **30%**
  - 现金可达 **100%**
- 评估初始资金：**100 万人民币**；评估时**每个盲测窗现金重启**。纸交易账户**连续结转**。

### 0.5 文件职责

| 文件 | 可否改 | 内容 |
|------|--------|------|
| `skill.md` | 否 | 本壳 |
| `strategy_genome.md` | 仅进化流程 | 战术参数与抽象触发 |
| `state/*` | 运行时 | 持仓、权益、信息包 |
| `evolution/*` | 运行时 | 窗口、候选、晋级日志 |

---

## 1. 决策哲学（冻结骨架，非点名）

执行时带着这些可检验原则（细节幅度读策略基因）：

1. **保本金优先**：不确定则偏不操作；宁可不做，不两头挨打。
2. **高盈亏比**：试错仓小；趋势确认后再加；破逻辑/破风控则走。
3. **资金先于叙事**：主题由资金连续流入 + 可验证催化长出，不由预设清单决定。
4. **角色清晰**：每笔持仓必须标明角色（进攻/防御/周期/工具）；角色决定持有节奏。
5. **马太与物极**：强者可更强，但已拥挤且失效时切换；无人问津且逻辑在时才考虑逆向。
6. **默认不操作**：无明确动机时输出「今日不操作」并写原因。

---

## 2. 日更流程（纸交易 + 信息包）

触发：交易日日程（由自动化任务调度）。默认在约定时刻运行。

### 2.1 按日信息包（防软穿越）

每个交易日维护一份 `state/info_packs/YYYY-MM-DD.json`（或等价），只含 **T 日 09:00 前可见** 数据：

**主回放路径（结构化最小包）**优先包含：

- 交易日历标记、指数与可交易标的的前收/开高低收（仅用已收盘日）；
- 板块/个股资金流向（已对齐日期的字段）；
- 公告元数据：标题、代码、发布时间戳（≤ T 09:00）；
- 持仓标的近况字段（价量、是否除权等可验证项）。

**叙事类新闻全文**不进主回放；仅在全真抽样日允许受控搜索，且每条必须有 ≤ T 09:00 的时间戳，否则丢弃。

历史段：能用带时间戳档案干净重建的重建；重建不上的用前瞻落盘。评估只认「包完整」的交易日。

### 2.2 盘前扫描（渠道，非清单）

被动扫描（每渠道先问「今天什么在动」）：

1. 海外隔夜市场与映射情绪（不预设永久龙头名）
2. 商品/利率等宏观价格波动（有数字才收）
3. 国内政策与监管要闻（事件名由搜索长出）
4. 产业催化（订单/涨价/产能等硬信息）
5. A 股公告（业绩/合同/股权/立案等）

主动扫描（服务调仓，读策略基因阈值）：

1. 持仓状态：目标/止损/除权/资金是否连续流出
2. 当前主线内相对强弱与补涨候选（主线名当日动态，不写死）
3. 同主题内「业绩可验证」替代是否存在
4. 新方向是否出现（非持仓主题）
5. 高占比角色的资金连续性

任一渠道无增量则跳过，禁止硬凑。

### 2.3 决策与成交记账

1. 加载 `state/portfolio.json`（连续账户）与当前 `strategy_genome.md`。
2. 用 §1 + 策略基因生成调仓；每笔必须满足 §5 产出契约。
3. 强制执行硬顶；越界则削减或取消该笔。
4. 纸交易记账：信号记在 T，**默认按 T+1 开盘价**成交并扣成本；若自动化在盘中运行且用户另有约定，须在状态文件中注明成交规则版本。
5. 更新持仓、权益曲线、当日决策日志 `state/decisions/YYYY-MM-DD.md`。
6. 无动机 → 明确「今日不操作」。
7. **必做 §2.5 自进化反思**（日更产物，供进化加权，日更本身不改策略基因）。

### 2.5 日更自进化反思（权重大于历史回溯）

每个交易日在决策与记账后，写入 `evolution/live_reflections/YYYY-MM-DD.md`（及可选结构化摘要）。只许用**当日及此前**已实现信息，禁止用尚未发生的涨跌倒推。

反思必须结构化（禁止点名写回规则）：

1. **决策回顾**：今日操作/不操作的动机形态是否与策略基因一致；是否撞硬顶。
2. **已实现结果**：仅评估「昨日及更早信号」在今日可得价格下的实现盈亏（T+1 开盘规则），更新 `state/live_scorecard.json`（任期内扣成本年化、最大回撤、交易日数、胜率、违规次数）。
3. **误差归因（抽象）**：如风控过慢、换手过高、空仓过久、追涨倾向、该动未动——**不得**写成「应增加某细分/某股票」。
4. **突变提案（非生效）**：0–3 条对策略基因的**抽象**改参建议（字段级，如「G4.自高点强制止损」），写入 `evolution/live_proposals/`；标记来源为「实盘」、权重提示为「高」。提案**不直接改** `strategy_genome.md`，只供进化自动化按 §3.4 高权重采纳或拒绝。
5. **反例约束**：若今日反思与历史盲测归因冲突，注明冲突；进化侧默认**偏信实盘**（见 §3.4）。

### 2.4 卖/买/清/不操作动机（形态层，幅度在策略基因）

以下为**形态名**，禁止在此填写具体行业/股票：

**倾向卖/减**：目标到达；催化弱于预期；单票占比触及策略基因减压带；日内加仓层失效做 T；基本面瑕疵（业绩雷）；估值压制；主题资金连续流出；同主题弱换强；风控线触发。

**倾向买/加**：新方向建底仓；主线扩散至相对低位第二梯队；降波动需求开防御角色；补涨预期差；同主题换更高壁垒或换业绩可验证或换容量；除权后可参与的容量替代；分歧抗跌试错；关键均线/位置低吸（参数见策略基因）；早盘弱势承接（高开冲高不追）。

**倾向清**：止盈完成且技术失效；持续弱于同主题；逻辑破灭；合规/风控禁止；资金连续流出确认。

**倾向不动**：持仓逻辑与技术未坏；主题分歧未明；混沌期降频；无腾位需求且总仓已近优。

清 = 离场；减 = 落袋。逻辑退出不要写成象征性小减。

---

## 3. 进化流程（持续跑）

由独立自动化任务**持续调度**（高频/常驻）：

1. **高优先级**：消化日更反思 / 实盘提案 / 实盘记分卡（前瞻纸交易）；
2. **次优先级**：在全部可得 A 股历史上推进滚动前进盲测。

两路都不得偷用未来段；冲突时偏信实盘（§3.4）。

### 3.1 窗口状态

维护 `evolution/window.json`：

- 进化段起止（约 24 个月）
- 盲测段起止（6 个月）
- 现任基因版本
- 现任基因上的实盘交易日数
- 状态：突变中 / 评估中 / 待晋级 / 右移中

数据范围：从数据源最早可用交易日直到最近一个**已封闭**盲测窗终点；开放中的「今日」不得进入历史盲测标签。连续纸交易账户除外：它是前瞻路径，但反思只能用已实现成交。

### 3.2 一代步骤

1. **检查器**：确认壳未被改；策略基因无个股/细分点名；无「用未来涨跌倒推规则」文本。
2. **突变源混合**（配额冻结）：
   - 至少 **3/5** 的候选必须来自或明确响应近期「实盘」高权重提案/反思主题；
   - 至多 **2/5** 可来自纯历史窗归因；
   - 每候选仍为小改动（参数或一条抽象规则）。
3. **评估**：
   - `历史分`：同一盲测窗回放（信息包主路径 + 抽样全真日）；每窗现金重启 100 万；T→T+1 开盘 + 成本；
   - `实盘分`：读取 `state/live_scorecard.json` 与任期内反思合规性（点名违规、硬顶违规则重罚）。
4. **晋级**：计算综合分（§0.1）；通过双轨回撤门槛且综合分最高者晋级；若实盘样本不足最少交易日或无候选过线 → **保持现任**。
5. **右移**：历史窗在晋级或确认不晋级后右移 6 个月；纸交易账户**不重启**。历史未跑完则继续；到数据尽头则仍持续处理日更反思。
6. **落盘**：`evolution/log/` 记录候选来源（实盘/历史）、差异、实盘分、历史分、综合分、是否晋级；通过则写回 `strategy_genome.md` 并提升版本号。

### 3.3 归因隔离

进化代理可读：结构化错误归因（如「风控触发过晚」「换手过高」「空仓过久」）以及日更反思中的抽象提案。  
不可读/不可用：历史盲测段个股事后涨跌倒推「应买某类」；把实盘反思里的具体股票名写进策略基因。

### 3.4 实盘优先冲突决议

当实盘提案与历史盲测最优方向冲突时：

1. 默认采纳 **实盘** 侧方向生成候选（计入 3/5 配额）；
2. 历史侧最多保留 2/5 作对照；
3. 晋级仍看综合分与双轨回撤门槛——实盘权重大不意味着可以牺牲回撤硬顶。

---

## 4. 选股与组合（执行时动态，规则层不点名）

开仓前在候选上过四维（具体阈值读策略基因）：

1. **基本面**：地位清晰、业绩或订单可验证、非纯一日游炒作  
2. **技术面**：回调企稳 / 区间下沿 / 突破回踩等形态（参数在策略基因）  
3. **资金面**：主题资金具连续性，而非单日脉冲  
4. **角色互补**：进攻/防御/周期/工具波动互补，避免单一角色堆满硬顶  

选股时综合：容量（流动性与可加仓性）、弹性、熟悉度（自身持仓史）、回避已拥挤且失效的龙头。  
列出 2–3 个同主题候选并写排除理由，再下单——主题名来自当日数据，不来自策略基因清单。

---

## 5. 产出契约

### 5.1 调仓指令（每笔五要素）

```
【组合】调仓  HH:MM
【操作类型】【股票名】，仓位变化，指引价（买入时给）
原因：一句话（对应动机形态名 + 当日事实）
角色：进攻|防御|周期|工具
```

必须包含：股票名、操作类型、仓位变化数字、买入指引价、盘中时序。  
禁止只给方向不给数字。

### 5.2 日更结束时还必须写

- 当日信息包路径与是否完整  
- 持仓表快照（代码、占比、成本、角色、目标/止损）  
- 权益与回撤  
- 若无操作：明确「今日不操作」+ 原因  
- **§2.5 反思文件** + 更新后的实盘记分卡 + 可选实盘提案目录  

### 5.3 进化结束时还必须写

- 窗口日期、现任版本、5 个候选摘要（无点名）与来源（实盘/历史）  
- 各候选实盘分 / 历史分 / 综合分 / 双轨回撤  
- 晋级决定与策略基因新版本号（或「保持现任」）  
- 本轮消费了哪些日更反思/提案  

---

## 6. 自动化分工

| 自动化 | 节奏 | 职责 |
|--------|------|------|
| 日更 | 工作日约定时刻 | §2 信息包 + 纸交易 + 连续账户 + **§2.5 自进化反思/提案** |
| 进化 | 持续调度 | §3：优先消化实盘反思，其次历史滚动前进；用尽可得 A 股历史 |

二者共享仓库内策略基因与状态；进化晋级后日更自动读新基因。  
日更**不得**直接改策略基因（只写反思与提案）；进化不得改本壳。  
综合评分 **实盘权重 > 历史权重**（默认 0.70 / 0.30）。

---

## 7. 免责

产出基于公开信息与模型推断，仅供研究与纸交易，不构成投资建议。实盘风险自担。
