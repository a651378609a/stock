---
name: self-evolving-long-only
description: 自进化场内 ETF 多头交易技能。冻结执行壳负责防穿越、仅 ETF、交易明细与依据、硬顶风控、日更/进化/独立监督；可进化参数仅在 strategy_genome.md。
---

# 自进化多头技能（冻结执行壳）

本文件是**冻结壳**：防穿越、仅 ETF、交易日志、硬顶、流程与产出契约。  
可进化内容只允许出现在 `strategy_genome.md`（策略基因）。进化代理**不得**改本文件。  
**监督代理**只审计、可一票否决晋级，也不得改本壳。

配套：
- `strategy_genome.md` — 唯一可进化物
- `state/` — 持仓、权益、信息包、决策
- `evolution/` — 窗口、候选、**交易明细日志**、晋级日志
- `evolution/audit/` — 监督代理审计结论（防穿越与路径合规）

---

## 0. 宪法（不可进化）

### 0.1 目标与晋级

- **不划分牛/熊标签**。
- 进化信号分两路；**有足够实盘样本时，日更反思权重大于历史回溯**：
  - `权重_实盘` 默认 **0.70**；`权重_历史` 默认 **0.30**（有实盘样本时使用，合计为 1）。
  - **冷启动 / 实盘不足**：当现任基因上的连续纸交易日数 < `最少实盘交易日`（默认 **20**）时，**不阻塞晋级**。此时进入「纯历史晋级模式」：综合分 = `历史分`；只执行历史回撤门槛与压力月门槛；日更仍照常写反思/提案，供日后切换权重。
  - **实盘就绪**：交易日数 ≥ 20 后，综合分 = `0.70×实盘分 + 0.30×历史分`，并启用双轨回撤门槛。**禁止**为抬历史分而人为下调 `权重_实盘`。
- 代际晋级（评估器冻结）：
  1. 综合分（按上面模式计算）高于现任；
  2. **历史回撤硬门槛**：盲测段最大回撤 ≤ 现任历史 × (1+ε)，ε = **0.10**；
  3. **实盘回撤硬门槛**（仅实盘就绪时）：纸交易最大回撤 ≤ 现任纸交易 × (1+ε)；
  4. **压力月门槛**（见 §0.1b）：在数据已可得时强制考核。
- `历史分`：盲测段扣成本后的年化收益（标准化方法冻结在评估器）。
- `实盘分`：连续纸交易账户在「当前基因任期内」的扣成本年化收益 + 反思合规加减分（见 §2.5、§3.4）；样本不足时记为「未就绪」，不参与综合分、也不因此禁止历史晋级。
- 成本假设（评估器冻结）：佣金双边万三；印花税卖出千一；滑点 0.1%。
- 成交假设：信号日 T → **T+1 开盘价**成交，再叠加上述成本。

### 0.1b 压力月专项评测（冻结）

以下两个自然月为**固定压力切片**（市场回落较大，必须单独看，不得只看整窗平均）：

| 压力月 | 区间 |
|--------|------|
| 2026-04 | 2026-04-01～2026-04-30（交易日） |
| 2026-07 | 2026-07-01～2026-07-31（交易日） |

规则：

1. 每次历史评估（含滚动盲测与晋级对比）只要数据源已覆盖该月，就必须对现任与每一候选**单独输出**：该月扣成本收益、最大回撤、交易次数、是否触发硬顶/风控。
2. **压力月门槛**（数据已覆盖时生效）：候选在每一压力月的最大回撤 ≤ 现任同月最大回撤 × (1+ε)；若现任该月无样本，则候选该月最大回撤不得超过评估器冻结上限（默认 **25%**），否则拒收该候选。
3. 压力月表现只用于评测与晋级门槛，**禁止**据此在策略基因里点名股票或细分行业；归因只能写抽象形态（如「回撤期降仓过慢」「混沌期交易过频」）。
4. 日更若落在压力月内，反思须额外标注「压力月样本」，提案权重提示仍可为「高」。

### 0.2 时间协议（防穿越）

- **严格滚动前进**：进化可用段约 24 个月 → 盲测 6 个月 → 窗口右移 6 个月。
- 改策略基因的理由**不得**引用盲测段内的涨跌结果或未来信息。
- 进化可用**全部可得的历史 A 股数据**，但必须按滚动前进切开；禁止用全样本一次性拟合后再在同段上宣称盲测。
- 盘前决策只能使用 **当日 09:00 前已存在** 的信息（见 §2）。

### 0.3 禁点名与禁正股（壳 + 策略基因同样生效）

**禁止交易与持有 A 股正股**（含可转债等非 ETF 品种）。宇宙仅限**场内 ETF**。

规则层（`skill.md` / `strategy_genome.md` / 进化差异理由）禁止：

- 具体正股名 / 正股代码；
- 把细分行业/概念写成**固定偏好或永久优先级**；
- 把某一只 ETF 写成「永远必买/永不满仓」。

允许：

- **抽象 ETF 角色**：进攻成长 / 次线卫星 / 防御红利或低波 / 宽基工具 / 货币或短债 / 现金；
- **运行时**在产出与交易日志中写明具体 ETF 名称与代码（执行结果，不是规则）；
- 评估器内部的「角色→流动性 ETF 报价代理」映射表（`evolution/data/etf_role_map.json`），不得抄进策略基因当偏好清单。

### 0.4 交易宇宙与硬顶（冻结）

- 宇宙：**仅 A 股场内 ETF，多头**；可空仓；无融资融券、无港美、**无正股**。
- 硬顶：单只 ETF ≤ **20%**；总仓 ≤ **95%**；单日单向调仓 ≤ **30%**；现金可达 **100%**。
- 评估初始资金：**100 万人民币**；盲测窗现金重启；纸交易账户连续结转。

### 0.5 文件职责

| 文件 | 可否改 | 内容 |
|------|--------|------|
| `skill.md` | 否 | 本壳 |
| `strategy_genome.md` | 仅进化流程 | 战术参数与抽象触发 |
| `state/*` | 日更/进化 | 持仓、权益、信息包、决策 |
| `evolution/log/*` | 进化 | 代际摘要 |
| `evolution/trades/*` | 进化/日更 | **逐笔交易明细与依据（必填）** |
| `evolution/audit/*` | 仅监督代理 | 防穿越与路径审计 |

---

## 1. 决策哲学（冻结骨架，非点名）

执行时带着这些可检验原则（细节幅度读策略基因）：

1. **保本金优先**：不确定则偏不操作；宁可不做，不两头挨打。
2. **高盈亏比**：试错仓小；趋势确认后再加；破逻辑/破风控则走。
3. **资金先于叙事**：主题由资金连续流入 + 可验证催化长出，不由预设清单决定。
4. **角色清晰**：每笔 ETF 持仓必须标明抽象角色；角色决定持有节奏。
5. **马太与物极**：强者可更强，但已拥挤且失效时切换；无人问津且逻辑在时才考虑逆向。
6. **默认不操作**：无明确动机时输出「今日不操作」并写原因。
7. **先依据、后下单**：任何买入/卖出必须能追溯到**信号日之前**已存在的盘面与消息面证据（见 §2.6）。

---

## 2. 日更流程（纸交易 + 信息包）

触发：交易日日程（由自动化任务调度）。默认在约定时刻运行。

### 2.1 按日信息包（防软穿越）

每个交易日维护一份 `state/info_packs/YYYY-MM-DD.json`（或等价），只含 **T 日 09:00 前可见** 数据：

**主回放路径（结构化最小包）**优先包含：

- 交易日历；宽基/风格指数与**候选场内 ETF** 的前收/开高低收（仅已收盘日）；
- ETF/板块资金流向（已对齐日期）；
- 公告与宏观要闻元数据：标题、来源、**发布时间戳 ≤ T 09:00**；
- 持仓 ETF 近况（价量、溢价折价、流动性）。

**叙事类新闻全文**须带时间戳；无法证明 ≤ T 09:00 的一律丢弃。  
历史段：能干净重建的重建；否则前瞻落盘。评估只认「包完整」交易日。

### 2.2 盘前扫描（渠道，非清单；标的仅 ETF）

被动扫描：海外隔夜、商品/利率、国内政策、产业催化、与 ETF 相关的规模/份额/溢价异常。  
主动扫描：持仓 ETF 状态、角色内相对强弱、资金连续性、是否需在 ETF 角色间平换。  
**禁止**扫描后改去买正股。无增量则跳过。

### 2.3 决策与成交记账

1. 加载 `state/portfolio.json` 与 `strategy_genome.md`（持仓只能是 ETF）。
2. 用 §1 + 策略基因生成**仅 ETF**调仓；每笔满足 §2.6 与 §5。
3. 强制硬顶；越界则削减或取消。
4. 信号在 T，默认 T+1 开盘成交 + 成本。
5. 更新持仓、权益、`state/decisions/YYYY-MM-DD.md`，并追加 `evolution/trades/` 交易明细（§2.6）。
6. 无动机 → 「今日不操作」+ 原因。
7. 必做 §2.5 反思。

### 2.6 交易明细与依据（冻结，日志必显）

**没有依据的交易视为无效**，进化日志若只有参数表没有逐笔明细，监督代理必须判不合格。

每笔成交（日更与历史回放共用字段）必须写入 `evolution/trades/YYYY-MM-DD.md`（或代际汇总 `evolution/trades/代数_*.md`），至少包含：

| 字段 | 要求 |
|------|------|
| 信号日 / 成交日 | 信号 T，成交默认 T+1 |
| ETF 名称与代码 | 必须是场内 ETF；禁止正股 |
| 操作 | 开/加/减/清/平换 |
| 仓位变化与价格 | 占比数字 + 成交价 |
| 角色 | 进攻成长/次线/防御/工具/货币等 |
| 盘面依据 | **仅用信号日及之前**的价量、均线/相对强弱、资金流等，写清观察窗口 |
| 消息面依据 | **仅用发布时间戳 ≤ 信号日 09:00** 的政策/宏观/ETF 相关信息；无可靠消息则写「消息面不足，本次仅依据盘面」或选择不交易 |
| 动机形态 | 对应 §2.4 形态名 |
| 基因条款 | 触发了哪些 G 节阈值（可选） |

禁止：用成交日之后才知道的涨跌倒推「早就该买」；用无时间戳自媒体小作文；日志只写「按模型调仓」而无盘面/消息面要点。

### 2.5 日更自进化反思（权重大于历史回溯）

每个交易日在决策与记账后，写入 `evolution/live_reflections/YYYY-MM-DD.md`（及可选结构化摘要）。只许用**当日及此前**已实现信息，禁止用尚未发生的涨跌倒推。

反思必须结构化（禁止点名写回规则）：

1. **决策回顾**：今日操作/不操作的动机形态是否与策略基因一致；是否撞硬顶。
2. **已实现结果**：仅评估「昨日及更早信号」在今日可得价格下的实现盈亏（T+1 开盘规则），更新 `state/live_scorecard.json`（任期内扣成本年化、最大回撤、交易日数、胜率、违规次数）。
3. **误差归因（抽象）**：如风控过慢、换手过高、空仓过久、追涨倾向、该动未动——**不得**写成「应增加某细分/某股票」。
4. **突变提案（非生效）**：0–3 条对策略基因的**抽象**改参建议（字段级，如「G4.自高点强制止损」），写入 `evolution/live_proposals/`；标记来源为「实盘」、权重提示为「高」。提案**不直接改** `strategy_genome.md`，只供进化自动化按 §3.4 高权重采纳或拒绝。
5. **反例约束**：若今日反思与历史盲测归因冲突，注明冲突；进化侧默认**偏信实盘**（见 §3.4）。

### 2.4 卖/买/清/不操作动机（形态层，幅度在策略基因）

以下为**形态名**，禁止在此填写具体行业/股票：

**倾向卖/减**：目标到达；催化弱于预期；单票占比触及策略基因减压带；日内加仓层失效做 T；基本面瑕疵（业绩雷）；估值压制；主题资金连续流出；同主题弱换强；风控线触发。

**倾向买/加**：新方向建底仓；主线扩散至相对低位第二梯队；降波动需求开防御角色；补涨预期差；同主题换更高壁垒或换业绩可验证或换容量；除权后可参与的容量替代；分歧抗跌试错；关键均线/位置低吸（参数见策略基因）；早盘弱势承接（高开冲高不追）。

**倾向清**：止盈完成且技术失效；持续弱于同主题；逻辑破灭；合规/风控禁止；资金连续流出确认。

**倾向不动**：持仓逻辑与技术未坏；主题分歧未明；混沌期降频；无腾位需求且总仓已近优。

清 = 离场；减 = 落袋。逻辑退出不要写成象征性小减。

---

## 3. 进化流程（持续跑）

由独立自动化任务**持续调度**（高频/常驻）：

1. **高优先级**：消化日更反思 / 实盘提案 / 实盘记分卡（前瞻纸交易）；
2. **次优先级**：在全部可得 A 股历史上推进滚动前进盲测。

两路都不得偷用未来段；冲突时偏信实盘（§3.4）。

### 3.1 窗口状态

维护 `evolution/window.json`：

- 进化段起止（约 24 个月）
- 盲测段起止（6 个月）
- 现任基因版本
- 现任基因上的实盘交易日数
- 状态：突变中 / 评估中 / 待晋级 / 右移中

数据范围：从数据源最早可用交易日直到最近一个**已封闭**盲测窗终点；开放中的「今日」不得进入历史盲测标签。连续纸交易账户除外：它是前瞻路径，但反思只能用已实现成交。

### 3.2 一代步骤

1. **检查器**：确认壳未被改；策略基因无个股/细分点名；无「用未来涨跌倒推规则」文本。
2. **突变源混合**：
   - **有可用实盘提案/反思时**：至少 **3/5** 候选响应实盘；至多 **2/5** 纯历史。
   - **无实盘样本或尚无实盘提案时（冷启动）**：允许 **5/5** 均来自历史窗归因与压力月抽象归因；**不得**因缺少实盘分而跳过突变/晋级。
   - 每候选仍为小改动（参数或一条抽象规则）。
3. **评估**：
   - `历史分`：同一盲测窗回放；每窗现金重启 100 万；T→T+1 开盘 + 成本；
   - **压力月切片**：按 §0.1b 对 2026-04、2026-07 单独计分（数据可得时）；
   - `实盘分`：若记分卡交易日数 ≥ 20 则计算；否则标记「未就绪」，综合分退回纯历史模式。
4. **晋级**：按 §0.1 / §0.1b 计算综合分与门槛；通过者取综合分最高晋级；若无候选过线 → **保持现任**（不是因为缺实盘分，而是因为没人过历史/压力月门槛）。
5. **右移**：历史窗在晋级或确认不晋级后右移 6 个月；纸交易账户**不重启**。历史未跑完则继续；到数据尽头则仍持续处理日更反思。
6. **落盘**：`evolution/log/` 必须含：模式（纯历史 / 实盘加权）、候选来源、历史分、实盘分或「未就绪」、**压力月两月分项表**、综合分、是否晋级；通过则写回 `strategy_genome.md` 并升版本。

### 3.3 归因隔离

进化代理可读：结构化错误归因（如「风控触发过晚」「换手过高」「空仓过久」「压力月回撤失控」）以及日更反思中的抽象提案。  
不可读/不可用：用压力月或盲测段个股事后涨跌倒推「应买某类」；把实盘反思里的具体股票名写进策略基因。

### 3.4 实盘优先冲突决议

**仅当实盘就绪（交易日数 ≥ 20）且存在实盘提案时**适用：

1. 默认采纳 **实盘** 侧方向生成候选（计入 3/5 配额）；
2. 历史侧最多保留 2/5 作对照；
3. 晋级仍看综合分、历史/实盘回撤门槛与压力月门槛。

冷启动阶段不适用本条，以历史分 + 压力月门槛为主。

---

## 4. 选股与组合（执行时动态，规则层不点名）

开仓前在候选上过四维（具体阈值读策略基因）：

1. **基本面**：地位清晰、业绩或订单可验证、非纯一日游炒作  
2. **技术面**：回调企稳 / 区间下沿 / 突破回踩等形态（参数在策略基因）  
3. **资金面**：主题资金具连续性，而非单日脉冲  
4. **角色互补**：进攻/防御/周期/工具波动互补，避免单一角色堆满硬顶  

选股时综合：容量（流动性与可加仓性）、弹性、熟悉度（自身持仓史）、回避已拥挤且失效的龙头。  
列出 2–3 个同主题候选并写排除理由，再下单——主题名来自当日数据，不来自策略基因清单。

---

## 5. 产出契约

### 5.1 调仓指令（每笔五要素）

```
【组合】调仓  HH:MM
【操作类型】【股票名】，仓位变化，指引价（买入时给）
原因：一句话（对应动机形态名 + 当日事实）
角色：进攻|防御|周期|工具
```

必须包含：股票名、操作类型、仓位变化数字、买入指引价、盘中时序。  
禁止只给方向不给数字。

### 5.2 日更结束时还必须写

- 当日信息包路径与是否完整  
- 持仓表快照（代码、占比、成本、角色、目标/止损）  
- 权益与回撤  
- 若无操作：明确「今日不操作」+ 原因  
- **§2.5 反思文件** + 更新后的实盘记分卡 + 可选实盘提案目录  

### 5.3 进化结束时还必须写

- 晋级模式：纯历史 / 实盘加权  
- 窗口日期、现任版本、5 个候选摘要（无点名）与来源（实盘/历史）  
- 各候选：历史分、实盘分或「未就绪」、综合分、历史回撤、实盘回撤（若有）  
- **压力月分项表（必填，数据可得时）**：2026-04 与 2026-07 各自的扣成本收益、最大回撤、交易次数  
- 晋级决定与策略基因新版本号（或「保持现任」及原因）  
- 本轮消费了哪些日更反思/提案（可为空）  

---

## 6. 自动化分工

| 自动化 | 节奏 | 职责 |
|--------|------|------|
| 日更 | 工作日约定时刻 | §2：仅 ETF 纸交易 + 信息包 + **交易明细依据** + §2.5 反思 |
| 进化 | 持续调度 | §3：ETF 滚动前进；纯历史或实盘加权；压力月；写出交易明细 |
| **监督** | 进化之后或每小时错峰 | §8：独立审计防穿越与优化路径；**可一票否决晋级** |

日更不改基因；进化不改壳；监督不改壳与基因内容，只写 `evolution/audit/` 并在否决时回滚未监督确认的晋级。

---

## 7. 免责

产出基于公开信息与模型推断，仅供研究与纸交易，不构成投资建议。实盘风险自担。

---

## 8. 独立监督代理（冻结，最重要）

必须由**与进化/日更分离**的自动化任务执行。监督代理不得参与突变提案，不得为抬分而改规则。

### 8.1 防穿越审计（最高优先级）

对最近一代 `evolution/log/*`、`evolution/trades/*`、日更决策与信息包抽样核查：

1. 每笔交易的盘面/消息面依据时间戳是否均 ≤ 信号日 09:00；
2. 改基因理由是否引用了盲测段内未来涨跌或压力月事后叙事倒推；
3. 信息包是否混入信号日盘中/盘后才发布的内容；
4. 是否出现正股成交。

任一条成立 → **审计失败**，否决本代晋级；若基因已被改写，监督代理应恢复为晋级前版本（或标记 `待人工回滚`）并写明证据。

### 8.2 优化路径合规

确认本代同时满足：

- 晋级模式正确（纯历史 / 实盘加权）；
- 未因「无实盘分」错误禁晋级，也未在实盘未就绪时伪造实盘分；
- 仅 ETF；硬顶未破；
- 日志含**逐笔交易与依据**，非只有参数表；
- 压力月 2026-04、2026-07 已单独出表（数据可得时）；
- 突变配额与禁点名规则未被绕过；
- 综合分/回撤门槛计算与 §0.1 一致。

### 8.3 产出

写入 `evolution/audit/YYYYMMDDTHHMMSSZ_audit.md`（及 json）：

- 结论：通过 / 否决
- 防穿越证据（文件路径 + 摘录）
- 路径合规逐条勾选
- 对进化自动化的强制动作：允许晋级生效 / 否决并回滚 / 要求补交易明细后重跑

**未经最近一次「通过」审计，晋级不得视为最终生效。**
